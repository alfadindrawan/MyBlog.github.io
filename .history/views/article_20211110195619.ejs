<!DOCTYPE html>
<html>

<head>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta charset="utf-8">
  <title>BLOG</title>
  <!-- <link rel="stylesheet" href="css/bootstrap.css"> -->
  <link rel="stylesheet" href="/css/style.css">
  <!-- <script src="/send_url.js"></script> -->
</head>

<body>
  <!-- <script type="text/javascript" src="js/bootstrap.js"></script> -->


    <main>
      <div class="article">
        <% if(article.category==='all' ) { %>
          <h1>
            <%= article.title %>
          </h1>
          <img src=`<%= article.image %>` alt="">
          <p>
            <%= article.content %>
          </p>
          <% } %>

            <% if(article.category==='limited' ){ %>
              <i>KHUSUS ANGGOTA</i>
              <h1>
                <%= article.title %>
              </h1>

              <% if(locals.isLoggedIn){ %>
                <p>
                  <%= article.content %>
                </p>
                <% }else{ %>
                  <div class="article-login">
                    <p>Ayo login sekarang!</p>
                    <p>Silahkan login untuk membaca<br>artikel selengkapnya.</p>
                    <!-- <img src="/images/login.svg"> -->
                    <img src="/images/open-door.png" class="login-img">
                    <a class="butt" href="/login">Login</a>
                  </div>
                  <% } %>
                    <% } %>

      </div>
    </main>
    <footer>
      <a class="butt sub" href="/list">Kembali ke Artikel</a>
    </footer>
</body>

</html>